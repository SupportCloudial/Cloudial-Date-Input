<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>FinalPrice__c</fullName>
    <formula>IF(
  Access_Type__c = &quot;Usage Based&quot;,
  IF(ISBLANK(ListPrice__c), 0, ListPrice__c),
  IF(
    OR(ISBLANK(Final_Adjusted_Price__c), Final_Adjusted_Price__c = 0),
    Price__c * (1 + Discount__c),
    Final_Adjusted_Price__c
  )
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Claculated Price</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Currency</type>
</CustomField>
